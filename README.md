# Controller Diagnostic

Browser-based controller input diagnostics focused on values exposed by the Web Gamepad API.

Initial MVP targets a universal controller tester validated with a PS4 DualShock 4 plus synthetic Gamepad data. The project avoids claiming hardware failure and separates standard mapping from raw input mode.
